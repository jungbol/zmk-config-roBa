description: Custom input processor for acceleration

compatible: "zmk,input-processor-acceleration"

include: base.yaml

properties:
  acceleration-exponent:
    type: int
    required: true

  acceleration-coefficient:
    type: int
    required: true

# 🔥 이게 정답 (아무 값도 넣지 않음)
input-processor-cells:
